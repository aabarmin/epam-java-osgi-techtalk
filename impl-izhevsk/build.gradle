apply plugin: 'java'

sourceCompatibility = 1.9
targetCompatibility = 1.9

dependencies {
    compile project(':service')
}

ext.moduleName = 'epam.techtalk.impl.izhevsk'

compileJava {
    inputs.property("moduleName", moduleName)
    doFirst {
        options.compilerArgs = [
                '--module-path', classpath.asPath
        ]
        classpath = files()
    }
}
